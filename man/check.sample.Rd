% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.sample.R
\name{check.sample}
\alias{check.sample}
\title{Print statistic and visualize one gene-SNV pair.}
\usage{
check.sample(sample_gene, sample_snp, plottype = "boxplot",
  removeZero = TRUE)
}
\arguments{
\item{sample_gene}{gene expression vector that each element in it is the gene expression level in one sample}

\item{sample_snp}{snp vector that each element in it is the genotype in one sample}

\item{plottype}{an optional parameter used to choose the type of plot, candidates are \code{boxplot}, \code{violin}, \code{histplot}. \code{boxplot} is used if not specified.}

\item{removeZero}{an optional parameter used to decide if you'd like to exclude non-expressed samples when plot the sample. Setting to \code{TRUE} helps to check non-zero part difference while setting to \code{FALSE} helps to check zero ratio difference.\code{TRUE} is used if not specified.}
}
\value{
NONE.
output the mean of each group, non-zero part mean of each group, non-zero part standard error of each group to the terminal and a picture show the relationship between gene and snp just like picture below.
}
\description{
Print statistic and visualize one gene-SNV pair.
}
\note{
Box Plot \figure{boxplot.png}
}
\examples{
check.sample(gene[1,], snp[1,])
check.sample(gene[1,], snp[1,], 'histplot')
check.sample(gene[1,], snp[1,], 'histplot', FALSE)
}
